# NFT 多层级路由重构说明

## 🚀 快速启动

```bash
cd frontend
npm run dev
```

访问: http://localhost:5173/nft

## 📁 新的路由结构

```
/nft                    -> 数据概览 (默认)
├── /nft/dashboard      -> 数据概览
├── /nft/assets         -> 资产管理
├── /nft/minting        -> 铸造任务
├── /nft/history        -> 铸造历史
└── /nft/contracts      -> 合约管理
```

## 🎨 布局特性

- **侧边栏导航**: 固定左侧，5个菜单项
- **面包屑导航**: 显示当前路径
- **响应式**: 支持移动端
- **主题**: 赛博朋克风格

## 📂 文件结构

```
src/pages/NFT/
├── components/
│   ├── NFTLayout.tsx       # 布局组件
│   └── NFTLayout.less
├── dashboard/              # 数据概览
├── assets/                 # 资产管理
├── minting/                # 铸造任务
├── history/                # 铸造历史
├── contracts/              # 合约管理
└── index.tsx              # 旧页面（保留）

src/router/
├── index.tsx              # 主路由配置
└── routes/
    └── nft.tsx            # NFT子路由配置

src/hooks/
└── useNFT.ts              # NFT相关hooks
```

## ✅ 已完成的功能

1. ✅ 多层级路由配置
2. ✅ 布局组件（侧边栏+面包屑）
3. ✅ 5个子页面（Dashboard、Assets、Minting、History、Contracts）
4. ✅ 赛博朋克主题样式
5. ✅ TypeScript类型支持
6. ✅ 响应式设计

## 📝 注意事项

1. 当前使用的是模拟数据（MOCK数据），需要替换为真实API调用
2. 部分功能（如批量铸造、合约部署）需要后端API支持
3. 钱包连接功能需要集成Web3库（如ethers.js或wagmi）

## 🐛 调试技巧

```bash
# 运行类型检查
npx tsc --noEmit

# 运行ESLint检查
npm run lint

# 构建测试
npm run build
```

## 📞 需要帮助？

查看以下文件获取更多信息：

- `src/router/routes/nft.tsx` - 路由配置
- `src/pages/NFT/components/NFTLayout.tsx` - 布局组件
- `src/hooks/useNFT.ts` - 数据获取hooks
